#include <stdio.h>
#include <iostream>
using namespace std;

int main() {
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);

    int produto = a * b;

    cout << "PROD = " << produto << endl;
    return 0;
}