#include <stdio.h>
#include <iostream>
using namespace std;

int main() {
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);

    int soma = a + b;

    cout << "SOMA = " << soma << endl;
    return 0;
}