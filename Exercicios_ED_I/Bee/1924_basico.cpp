#include <stdio.h>
#include <iostream>
using namespace std;

main() {
    int i = 0, n;
    cin >> n;
    cin.ignore();

    while (i < n) {
        string entrada;
        cin >> entrada;
        i++;
    }
    cout << "Ciencia da Computacao" << endl;
}